<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Two Tomatoes</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
     <% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
  <% end %>
    <link rel="stylesheet" href="//use.fontawesome.com/releases/v5.0.7/css/all.css">
    <link href="https://fonts.googleapis.com/css?family=Lobster&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Muli&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body>
    <div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
      <div class="brand-container">
        <a class="navbar-brand" href="<%= root_path %>">
           <img src="/assets/new_tt_logo.png" alt="logo" />
        </a>
      <!--   <p>TwoTomatos</p> -->
      </div>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <% if !current_user || current_user.chef.nil? %>
            <li class="nav-item">
              <%= link_to "Become a Chef", new_chef_path, class: "nav-link" %>
            </li>
          <% else %>
          <% end %>
          <li class="nav-item">
            <%= link_to "Find a Chef", chefs_path, class: "nav-link" %>
          </li>

          <% if current_user && current_user.image %>
            <li class="nav-item dropdown">
                <%= image_tag current_user.image, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                  <%= link_to "Profile", users_show_path(current_user), class: "dropdown-item" %>
                  <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              <% else %>
                <li class="nav-item">
                  <%= link_to "Log in", users_show_path(current_user), class: "nav-link" %>
                </li>
              </div>
            </li>
          <% end %>
        </ul>
      </div>
    </div>






    <%= yield %>

    <% if params[:controller] == "chefs" && params[:action] == "show" %>
      <div></div>
    <% else %>
<!--       <p class="notice notice-color"><%= notice %></p> -->
      <p class="alert"><%= alert %></p>
      <%= javascript_include_tag 'application' %>
      <%= javascript_pack_tag 'application' %>
      <div class="footer">
        <div class="footer-links">
          <a href="#"><i class="fab fa-instagram"></i></a>
          <a href="#"><i class="fab fa-facebook"></i></a>
          <a href="#"><i class="fab fa-twitter"></i></a>
        </div>
        <div class="footer-copyright">
          This website was made with <i class="fas fa-heart"></i> by Ecopal Team
        </div>
      </div>
    <% end %>




  </body>
</html>
